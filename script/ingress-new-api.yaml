apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: new-api
  namespace: ai
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - pg.cocox.ai
      secretName: cocox-ai-wildcard-cert-manager
  rules:
    - host: pg.cocox.ai
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: new-api
                port:
                  number: 3000
